import java.util.LinkedList;

public class Book {

    String title;
    String callNum;
    int timesOut;
    boolean isAvailable;

    public Book(String title, String callNum, int timesOut, boolean isAvailable) {
        this.title = title;
    }
}

/**
 * Searches for a book with the matching title
 * @param myTitle The title we're searching
 */


/**
 * A collection of books (documentation for a class) --> for class library
 * don't need documentation for constructors
 * no comments in examples class
 */

